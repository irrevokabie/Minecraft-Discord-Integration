# Minecraft Discord Integration

Setup:
- Replace `webhook_url` with your Discord webhook.
- Replace `log_path` with where your server hoster saves the `latest.log` file. (For Paper MC: `logs/latest.log`
