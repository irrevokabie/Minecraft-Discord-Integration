# Minecraft Discord Integration

Setup:
1. Replace `webhook_url` with your Discord webhook
2. Replace `log_path` with where your server software saves the `latest.log` file.
    - For PaperMC : `logs/latest.log`
